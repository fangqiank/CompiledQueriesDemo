services:
  postgres:
    image: postgres:16
    container_name: ef_compiled_queries_pg
    environment:
      POSTGRES_DB: compiled_queries_demo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: